spring:
  application:
    name: service-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/JuanPLaverde08/config-data
  security:
    user:
      name: admin
      password: admin
server:
  port: 7010
